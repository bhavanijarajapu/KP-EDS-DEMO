version: 1

indices:
  contact-details:
    include:
      - /fragments/contact-details/**
    exclude:
      - /fragments/contact-details
    properties:
      title:
        select: head > title
        value: text
      description:
        select: head > meta[name="description"]
        attribute: content
    target: /fragments/contact-details/query-index.json
